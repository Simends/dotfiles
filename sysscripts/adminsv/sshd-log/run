#!/bin/execlineb -P
ifelse { test -w /var/log } {
	foreground { install -d -o s6log -g s6log /var/log/sshd }
	s6-setuidgid s6log exec -c s6-log -d3 -b -- n3 s2000000 T /var/log/sshd
}
foreground { install -d -o s6log -g s6log /run/log/sshd }
s6-setuidgid s6log exec -c s6-log -d3 -b -- n3 s2000000 T /run/log/sshd
